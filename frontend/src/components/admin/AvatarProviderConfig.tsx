/**
 * AvatarProviderConfig.tsx — Legacy component (no longer used).
 *
 * Avatar configuration is now handled inline within the per-bot editor at
 * /admin/bot/[id] (Avatar Setup tab). This file is kept to avoid orphan
 * imports but is not rendered anywhere in the current codebase.
 */

export default function AvatarProviderConfig() {
  return null;
}
