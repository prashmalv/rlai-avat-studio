/**
 * DocumentManager.tsx — Legacy component (no longer used).
 *
 * Document management is now handled inline within the per-bot editor at
 * /admin/bot/[id] (Knowledge Base tab). This file is kept to avoid orphan
 * imports but is not rendered anywhere in the current codebase.
 */

export default function DocumentManager() {
  return null;
}
